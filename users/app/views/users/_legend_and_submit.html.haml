%legend
  = t(legend)
= yield
.pull-right
  - if local_assigns[:with_cancel]
    = f.button :submit, :class => 'btn-primary'
    = link_to t(:cancel), root_url, :class => :btn
  - else
    = f.button :submit
.clearfix
